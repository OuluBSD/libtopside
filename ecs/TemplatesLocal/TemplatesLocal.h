#ifndef _TemplatesLocal_TemplatesLocal_h_
#define _TemplatesLocal_TemplatesLocal_h_

#include <EcsLib/EcsLib.h>



/*

- connectors
	- pipeconn ?
- components
	- customer (receiptsink, ordersrc)
	- input comps (sys-src)
		- sys-event (ordersink, eventsrc) e.g. midifile
		- sys-static (ordersink, staticsrc) e.g. jpg loader
		- sys-model...
		- sys-av (ordersink, audio-/videosrc)
	- convert-comps ...
		- audio-instrument (midisink, audiosrc)
	- pipe comps
		- audiopipe (audiosink, audiosrc)
	- endpoints
		- audio-endpoint (audiosink, receiptsrc)
		- display-endpoint
	
*/


#endif
