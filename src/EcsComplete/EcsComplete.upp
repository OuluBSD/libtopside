description "\377B0,131,159";

uses
	EcsLocal,
	EcsMinimal,
	IMidiHw,
	IMidiFile,
	EcsAudioCore;

uses(GUI) EcsVirtualGui;

uses(GUI) EcsCtrl;

uses(VR) EcsVR;

file
	EcsComplete.h,
	EcsComplete.cpp;

