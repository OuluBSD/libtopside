description "\377B128,0,255";

uses(!SCREEN) NonScreen;

uses(SCREEN & !GUI) NonCtrlSDL2;

uses(SCREEN & GUI) SDL2GUI3D;

uses(SCREEN & GUI) VirtualGui3D;

uses(SCREEN) AtomSDL2;

uses(!SCREEN) AtomNonScreen;

